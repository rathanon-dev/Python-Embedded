# Python Embedded Manager

เครื่องมือ PowerShell แบบง่ายสำหรับจัดการ Python (embedded) ผ่านแพ็คเกจ NuGet ช่วยให้คุณดาวน์โหลด, ติดตั้ง, และตั้งชื่อแทน (alias) Python ภายในโฟลเดอร์เฉพาะ โดยไม่รบกวน Python หลักที่ติดตั้งไว้ในเครื่อง

---

## 🚀 คุณสมบัติ

* แสดงรายการ Python (stable) ทั้งหมดที่มีบน NuGet
* ติดตั้ง Python แบบฝังตัว (embedded) ลงในโฟลเดอร์เฉพาะ (`python_embeded`)
* ติดตั้งซ้ำ หรือ อัปเกรด Python แบบฝังตัวที่มีอยู่
* ตั้งค่า alias สำหรับคำสั่ง `python` และ `pip` ให้เรียกใช้งานได้จากทุกที่ (globally)
* หน้าจอเมนู (UI) บน PowerShell ที่สะอาดตาและโต้ตอบได้

---

## 📋 ข้อกำหนด

* Windows 10/11
* PowerShell 7+ ขึ้นไป
* การเชื่อมต่ออินเทอร์เน็ต (สำหรับดาวน์โหลดแพ็คเกจ NuGet)

---

## 🔧 การติดตั้ง

1.  Clone หรือ ดาวน์โหลด repository นี้
2.  รันไฟล์ `RunAs.bat` เพื่อเปิดเครื่องมือนี้ด้วยสิทธิ์ผู้ดูแลระบบ (elevated privileges):
    ```bat
    RunAs.bat
    ```
    สคริปต์นี้จะเปิด PowerShell ด้วยสิทธิ์ผู้ดูแลระบบ (administrator rights) และรันไฟล์ `PythonNuget.ps1`

---

## ⌨️ การใช้งาน

1.  เลื่อนเมนูโดยใช้ปุ่ม **↑ / ↓**
2.  กด **Enter** เพื่อเลือก
3.  กด **Esc** เพื่อออกจากโปรแกรม
4.  หลังจากติดตั้ง Python เครื่องมือจะแสดงคำสั่งสำหรับใช้ตรวจสอบเวอร์ชัน Python และ pip ที่ติดตั้งแบบฝังตัว:
    ```powershell
    python -c "import sys, pip; print('Python version:', sys.version); print('pip version:', pip.__version__); print('Python executable:', sys.executable); print('pip module:', pip.__file__);"
    ```

---

## 📂 โครงสร้างไฟล์

```
PythonNuget/
│
├─ PythonNuget.ps1       # สคริปต์ PowerShell หลัก
├─ RunAs.bat               # ไฟล์ Batch สำหรับรันสคริปต์ PS แบบ elevated 
├─ README.md             # เอกสารนี้ ภาษาไทย
├─ README.EN.md          # เอกสารนี้ ภาษาอังกฤษ
├─ download/             # โฟลเดอร์สำหรับแพ็คเกจ NuGet และไฟล์ชั่วคราว 
└─ python_embeded/       # โฟลเดอร์สำหรับ Python แบบฝังตัวที่ติดตั้งแล้ว
```
## 📝 หมายเหตุ

* Python แบบฝังตัวจะถูกติดตั้งไว้ในโฟลเดอร์ `python_embeded` โดยไม่ยุ่งเกี่ยวกับ Python หลักของเครื่อง
* Alias ของ `pip` จะถูกตั้งค่าให้ชี้มายัง Python แบบฝังตัวนี้ (globally) หากคุณมี Python หลักติดตั้งอยู่ ระบบจะเตือนก่อนที่จะทำการเขียนทับ (override) alias
* เครื่องมือนี้จัดทำขึ้นเพื่อการใช้งานส่วนตัวหรือเพื่อการศึกษา
* **ความยืดหยุ่น:** คุณสามารถย้ายโฟลเดอร์โปรเจกต์นี้ทั้งหมด (หรือสร้างโฟลเดอร์ใหม่) ไปไว้ที่ไหนก็ได้ และสามารถตั้งชื่อโฟลเดอร์หลักเป็นอะไรก็ได้ตามต้องการ (เช่น `MyTools`, `PortablePy`) ไม่จำเป็นต้องเป็นชื่อ `PythonNuget` ครับ ตัวสคริปต์จะทำงานโดยอ้างอิงจากตำแหน่งที่มันถูกวางไว้
---

## 📄 สิทธิ์การใช้งาน (License)

โปรเจกต์นี้ใช้สิทธิ์การใช้งาน **PSF License 2.0** (Python Software Foundation License) เพื่อให้สอดคล้องกับแพ็คเกจของ Python โค้ดทั้งหมดใน repository นี้เป็นโอเพนซอร์สและใช้งานได้ฟรี